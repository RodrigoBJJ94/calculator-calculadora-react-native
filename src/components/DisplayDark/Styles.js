import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    display: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'flex-end',
        backgroundColor: '#000',
        padding: 20,
        width: '100%',
    },
    textResult: {
        fontSize: 50,
        color: '#fff',
    },
    textOperation: {
        fontSize: 35,
        color: '#fff',
    },
});
