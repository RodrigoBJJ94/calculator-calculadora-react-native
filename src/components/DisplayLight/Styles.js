import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    display: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'flex-end',
        backgroundColor: '#fff',
        padding: 20,
        width: '100%',
    },
    textResult: {
        fontSize: 50,
        color: '#000',
    },
    textOperation: {
        fontSize: 35,
        color: '#000',
    },
});
